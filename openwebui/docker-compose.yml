services:
  # =============================================================================
  # DATABASE SERVICE
  # =============================================================================
  
  open-webui:
    image: ghcr.io/open-webui/open-webui:v0.6.30
    container_name: open-webui
    ports:
      - "3000:8080"
    environment:
      - ENV=production
      - OPENAI_API_BASE_URL=http://openwebui-proxy:8001/v1
      - OPENAI_API_KEY=dummy-key
      - WEBUI_NAME=Siamtemp AI Assistant
      - WEBUI_URL=http://localhost:3000
      - DEFAULT_MODELS=llama3.1:8b
      - N8N_TIMEOUT=300
    volumes:
      - open_webui_data:/app/backend/data
    networks:
      - hvac_network
    restart: unless-stopped


volumes:
  open_webui_data:
# =============================================================================
# NETWORK
# =============================================================================

networks:
  hvac_network:
    driver: bridge
    name: hvac_network